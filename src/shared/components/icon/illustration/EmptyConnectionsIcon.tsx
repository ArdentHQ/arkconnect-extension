import ThemedIcon from '@/shared/components/icon/ThemedIcon';

export const EmptyConnectionsIcon = () => {
    const icon = 'connections-empty';
    return <ThemedIcon icon={icon} className='h-30 w-30' />;
};
