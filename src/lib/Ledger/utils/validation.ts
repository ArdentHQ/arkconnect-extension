export const hasRequiredAppVersion = async () => {
    // TODO fix logic
    // const coinId = coin.network().coin();
    //
    // if (minVersionList[coinId]) {
    //     const currentVersion = await coin.ledger().getVersion();
    //
    //     if (lt(currentVersion, minVersionList[coinId])) {
    //         return false;
    //     }
    // }

    return true;
};
